apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: nginx-a
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: nginx
        fleet: a
    spec:
      containers:
      - name: nginx
        image: nginx:1.11.1
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
